# Blockchain — minter wallet (never expose this, server-side only)
MINTER_PRIVATE_KEY=0x...

# Deployed contract address on Polygon Amoy Testnet
CONTRACT_ADDRESS=0x...

# Polygon Amoy RPC (public endpoint, safe to share)
POLYGON_AMOY_RPC_URL=https://rpc-amoy.polygon.technology

# Pinata JWT — create a key at app.pinata.cloud with "pinFileToIPFS" permission
PINATA_JWT=eyJ...
